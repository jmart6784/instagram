<div>  
  <h3><%= link_to "Home", posts_path %></h3>

  <% if user_signed_in? %>
    <h3>Welcome, <%= link_to ("#{current_user.first_name} #{current_user.last_name}").downcase.capitalize, current_user %></h3>
    <h3><%= link_to "Users", users_path %></h3>
    <h3><%= link_to "Settings", edit_user_registration_path %></h3>
    <h3><%= link_to "Log Out", destroy_user_session_path, method: :delete %></h3>
  <% else %>
    <h3><%= link_to "Sign Up", new_user_registration_url %>/<%= link_to "Log In", new_user_session_url %></h3>
  <% end %>
</div>