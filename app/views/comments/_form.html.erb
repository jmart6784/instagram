<% if @comment.errors.any? %>
  <div>
    <p><%= flash[:danger] %></p>
    <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><span><%= msg %></span></li>
      <% end %>
    </ul>
  </div>
<% end %>

<h2>Comment</h2>

<%= form_for [ @post, @comment ] do |f| %>
  <%= f.text_area :body, placeholder: "Share your thoughts", required: true %><br>


  <%= f.submit "Comment" %>
<% end %>